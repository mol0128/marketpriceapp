- if @items.present?
  - @items.each do |item|
    .result
      = image_tag item['mediumImageUrls'][0]
      = link_to item.name, item['itemUrl']
    .price
      = item.price
    %hr/
  = paginate @items