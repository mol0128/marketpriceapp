.container
  = form_with url:items_search_path, method: :get, local: true do |f|
    = f.text_field :keyword, placeholder: "型番を入力してください", autocomplete: 'off'
    = f.submit "検索" , :name => nil
    %p 
      ex)16610 中古
  %h2
    検索結果
    -# - if @items.present?
    -#   = @items.total_count
  = render partial: 'list'